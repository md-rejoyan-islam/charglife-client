import ContactContainer from "@/container/ContactContainer";
import React, { Suspense } from "react";

const Page = () => {
  return (
      <ContactContainer />
  );
};

export default Page;
